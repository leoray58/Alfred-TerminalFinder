# TerminalFinder Ghostty

Keywords:

- `ft`: Finder -> Terminal
- `tf`: Terminal -> Finder
- `fg`: Finder -> Ghostty
- `gf`: Ghostty -> Finder

Finder path resolution:

- Prefer the selected folder in Finder
- Fall back to the front Finder window directory
